# Codex App Server protocol

`0.144.4/` is generated by the pinned `codex-cli 0.144.4` App Server and is adapter-only.
It is not part of the Aegis public API and Provider fields from these schemas must never be copied
into `api/openapi.yaml`.

Regenerate and compare it with:

```sh
make codex-schema-check
```

