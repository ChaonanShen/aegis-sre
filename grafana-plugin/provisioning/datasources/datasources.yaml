apiVersion: 1

datasources:
  - name: Prometheus Local
    type: prometheus
    uid: prometheus-local
    access: proxy
    url: http://prometheus:9090
    # `prometheus` 是 Playbook 和 Canvas 示例使用的默认 datasource UID。
    isDefault: false
    editable: false
