services:
  grafana:
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
    build:
      args:
        grafana_image: grafana-enterprise
        grafana_version: 13.0.2
        development: 'false'
        anonymous_auth_enabled: 'false'
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: 'false'
      GF_AUTH_BASIC_ENABLED: 'true'
      GF_PATHS_CONFIG: /etc/grafana/torchbearing.ini
      # 地址是非敏感运行参数；服务凭据只通过只读文件传入，不进入浏览器配置。
      AEGIS_CONTROL_PLANE_URL: ${AEGIS_CONTROL_PLANE_URL:?required}
      AEGIS_CONTROL_PLANE_TOKEN_FILE: ${AEGIS_CONTROL_PLANE_TOKEN_FILE:-}
    volumes:
      - ./config/grafana.ini:/etc/grafana/torchbearing.ini:ro
